---
id: QC-018.07
title: Journal Commands
status: To Do
assignee: []
created_date: '2026-01-30'
labels:
  - application
  - analysis
  - research
  - P2
dependencies: [QC-017.06]
parent_task_id: QC-018
---

## Description

Application layer commands for Research Journal management.

## Acceptance Criteria

### CRUD Commands
- [ ] #1 `create_journal(name, content?, owner?) -> Result[JournalId, Error]`
- [ ] #2 `update_journal(journal_id, content) -> Result[None, Error]`
- [ ] #3 `rename_journal(journal_id, new_name) -> Result[None, Error]`
- [ ] #4 `delete_journal(journal_id) -> Result[None, Error]`
- [ ] #5 `get_journal(journal_id) -> Journal | None`
- [ ] #6 `list_journals(owner?) -> list[Journal]`

### Search Commands
- [ ] #7 `search_journals(query) -> list[SearchResult]`
- [ ] #8 `SearchResult`: journal, matches (with positions)

### Export Commands
- [ ] #9 `export_journal(journal_id, format, path) -> Result[Path, Error]`
- [ ] #10 `export_all_journals(format, path) -> Result[Path, Error]`
- [ ] #11 Supported formats: text, markdown, odt

### Auto-Save
- [ ] #12 `auto_save_journal(journal_id, content) -> None`
- [ ] #13 Debounced save (e.g., 1.5 seconds after last keystroke)

### Event Publishing
- [ ] #14 Publish domain events for all operations

## Implementation

- `src/application/analysis/journal_controller.py`

## Agent Tools

- `create_journal(name, content)` - Agent can create analysis notes
- `append_to_journal(journal_id, content)` - Agent can add to existing
- `search_journals(query)` - Agent can search research notes
