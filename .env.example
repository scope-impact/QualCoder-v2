# QualCoder v2 Environment Configuration
#
# Copy this file to .env.local and fill in your values.
# Do NOT commit .env.local to version control.

# ==============================================================================
# Convex Configuration (for cloud sync)
# ==============================================================================

# For Convex Cloud (convex.dev):
# CONVEX_URL=https://your-project-name.convex.cloud

# For Local Self-Hosted Convex (docker compose up):
CONVEX_URL=http://127.0.0.1:3210

# Admin key for self-hosted (generate with: docker compose exec backend ./generate_admin_key.sh)
# CONVEX_ADMIN_KEY=your_admin_key_here

# ==============================================================================
# Sync Mode
# ==============================================================================

# Backend mode: "sqlite" (local only), "convex" (cloud only), or "sync" (both)
BACKEND_MODE=sync
